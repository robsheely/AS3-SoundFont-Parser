package tonfall.core
{
    /**
    * Tonfall only supports a static blockSize (number of signals to be processed each callback)
    *
    * @see BlockInfo
    *
    * @author Andre Michelle
    */
    public const blockSize: int = 3072;
}
