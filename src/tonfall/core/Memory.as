package tonfall.core
{
    import flash.utils.ByteArray;
    /**
     * For sharing a ByteArray
     *
     * @author Andre Michelle
     */
    public const Memory:ByteArray = new ByteArray();
}
